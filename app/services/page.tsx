import React from 'react'
import { bebas_neue } from '../fonts/fonts'

const page = () => {
  return (
    <section className="services min-h-screen flex  flex-col items-center justify-center">
          <h1 className={`${bebas_neue.className} text-xl`}>Services</h1>
    </section>
  )
}

export default page